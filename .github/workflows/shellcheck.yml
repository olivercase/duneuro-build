name: shellcheck

on:
  push:
    branches: [main]
  pull_request:

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run shellcheck on build scripts
        run: shellcheck scripts/build_duneuro_local.sh cluster/build_duneuro.sh
